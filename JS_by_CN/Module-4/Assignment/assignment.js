// ==>
function test(){
    return this;
}
console.log(test()==window);  // true (run in browser console)

// ==>
console.log(window.navigator); // used to obtain browser's information.


// ==>
Element.innerHTML  //used to gets & sets the Text

// ==>
document.getElementsByTagName(name); //find an element by element id

// ==>
getElementsByClassName(),querySelectorAll(),getElementsByTagName() // returns list of elements 

// ==>
// Event Handler nothing but a function 


// ==>
// When a specified event occurs , the Browser invoke the handler.


// ==>
`when user clicks or moves over a document - mouse events generated` 

// ==>
'We can add script tag in head and body'

// ==>
'Advantages of using external javascript' 
// It seperate HTML and Code
// makes HTML and JS easier to read and maintain
// cached javascript files can speed up page loads


// ==>
deltaX  // Property that reports rotation of mouse wheel axis

// ==>
// KeyCode property holds `Digit` value when a shift key is pressed for adding punctuation Character


// ==>
// In event bubbling  event start from - 'deepest element or target element to its parents'


// ==>
// Event bubbling can be prevented by using `event.stopPropogation()`


//Assignment  ==>
// Type property specifies the type of event triggered

// ==>
// `drop and dragstart`  - are the events in JS 


// ==>
```

```